import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '멘토링 등록',
}

export default function MentoringCreateLayout({ children }: { children: React.ReactNode }) {
  return children
}
